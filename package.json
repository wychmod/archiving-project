{
  "name": "escontentgen",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "yarn electron .",
    "dev": "cross-env NODE_ENV=development yarn electron .",
    "build": "electron-builder --config build/electron-builder.js",
    "pack": "electron-builder --dir --config build/electron-builder.js"
  },
  "keywords": ["electron", "content", "generator"],
  "author": "",
  "license": "ISC",
  "description": "一个基于Electron的内容生成应用",
  "devDependencies": {
    "electron": "^35.1.4",
    "electron-builder": "^24.6.4",
    "cross-env": "^7.0.3"
  }
}
