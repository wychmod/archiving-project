{
  "name": "archiving-project-showcase",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "description": "Archiving Project · showcase site (Astro, framework-free rebuild)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:links": "node scripts/check-links.mjs",
    "check:tokens": "node scripts/check-tokens.mjs",
    "verify": "npm run check && npm run build && npm run check:tokens && npm run check:links",
    "gen:redirects": "python scripts/gen-redirects.py"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.18.2"
  },
  "optionalDependencies": {
    "@esbuild/win32-x64": "^0.28.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
